<!DOCTYPE html>
<html lang="en">
<head>
  <title>Followers</title>

  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.0/js/bootstrap.min.js"></script>
</head>

<body>
<img src="reftrkr.png" class="img-thumbnail" alt="logo" style="max-width: 50%; height: auto">
<div class="page-header">

</div>

<div class="container">
  <h2>Followers</h2>
  <table class="table table-striped table-bordered table-hover table-lg", content="width=device-width, initial-scale=1">
    <caption>List of users</caption>
    <thead>
      <style>
      table, th, td {
        padding: 10px;
        border: 1px solid black;
        border-collapse: collapse;
        text-align: center;
      }
    </style>

    <th>Email</th>
    <th>Counter</th>
    <th>User Id</th>
    <th>Referred By</th>
  </thead>
  <tbody>
    <% @user.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.counter %></td>
        <td><%= user.id %></td>
        <td><%=
          if @referral.find_by(referred: user.id) == nil
            "none"
          else
            @referrerId = @referral.find_by(referred: user.id).referrer
            User.find(@referrerId).email
          end
        %></td>
      </tr>
    </thead>
    <tbody>
      <% @user.each do |user| %>
        <tr>
          <td><%= user.email %></td>
          <td><%= user.counter %></td>
          <td><%= user.id %></td>
        </tr>
      <% end %>
      </tbody>
  </table>
</div>

</body>
</html>